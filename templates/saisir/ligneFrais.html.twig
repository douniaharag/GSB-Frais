<div class="col-sm">

                {% block contenu %}
                    <div class="jumbotron jumbotron-fluid">
                        <div class="container-fluid">
                            {#{{ form_start(formSaisir) }}
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col">
                                            {# formSaisir.1 = Forfait Etape (nuit et repas)#}
                                            {{ form_row(formSaisir.1) }}
                                        </div>
                                        <div class="col">
                                            {# form.2 = frais km#}
                                            {{ form_row(formSaisir.2) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col">
                                            {# form.3 = Nuitée Hotel#}
                                            {{ form_row(formSaisir.3) }}
                                        </div>
                                        <div class="col">
                                            {# form.4 = repas restaurant#}
                                            {{ form_row(formSaisir.4) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col">
                                            {{ form_row(formSaisir.save) }}
                                        </div>
                                        <div class="col">
                                            <button type="submit" class="btn btn-outline-success">Enregistrer</button>
									        <button type="reset" class="btn btn-outline-danger">Annuler</button>
                                        </div>
                                    </div>
                                </div>
                            {{ form_end(formSaisir) }}
                        </div>
                        #}
                    </div>

                {% endblock contenu %}
            </div>
            {# affichage sur le côté droit de la page#}
            <div class="col-sm-4 col-md-3">
                {% block affichage %}
                {% endblock affichage %}
            </div>
        </div>
    </div>